import { EntryFilterPipe } from './entry-filter.pipe';

describe('EntryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
